# Safe Core status

Safe Core public Windows CI is active on the `safe-core` branch. Physical-machine release remains blocked until the Windows PowerShell 5.1, WPF load, emergency Doctor, static safety policy, backend, and billing-guard jobs are green.
